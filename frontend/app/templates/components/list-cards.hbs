<section class="cards">
  {{#each card in sortedCards}}
    <div class="card"
         data-card-id={{card.id}}
         data-position="card_{{card.id}}">
      {{card.title}}
      {{card-comments card=card}}
    </div>
  {{/each}}
</section>
