<!DOCTYPE html>
<html>
<head>
  <title>Kanban</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="wrapper">

  <div id="header">
    <div class="header-logo">
      <h1><a href="#/">KANBAN <span>看板</span></a></h1>
    </div>
    <div class="header-user">
      <% if logged_in? %>
        <p>
          <!-- welcome back  -->
          <!-- <a href="#users/<%#= current_user.id %>"><%= current_user.email %></a> -->
          <!-- (<%#= link_to "logout", logout_url %>) -->
        </p>
        <%= link_to image_tag(current_user.gravatar_url(30)), "#" %> 
      <% else %>
        <p><%= link_to "login", login_url %>
      <% end %>
    </div>
  </div>

  <div id="main">
    <%= yield %>
  </div>

  <div id="footer"></div>

</div>

</body>
</html>
